# fitocracyextensionapp

A local file needs to be added, something like "credendials.txt", which contains the fitocracy username and password, in config style format.

cat credentials.txt

username:namea
password:pword